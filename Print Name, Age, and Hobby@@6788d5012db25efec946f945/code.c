#include <stdio.h>

int main() {
    string name, hobby;
    int age;
    scanf("%s %d %s", name, &age, hobby)
    printf("Name: %s\n",name);
    printf("Age: %d\n", age);
    printf("Hobby: %s\n",hobby);
    return 0;
}