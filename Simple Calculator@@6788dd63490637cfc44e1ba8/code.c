#include <stdio.h>
int main() {
    int num1, num2, sum;
    scanf("%d %d", &num1, num2);
    sum = (num1 + num2);
        printf("%d\n", sum);
    }
int main() {
    int num1, num2, difference;
    scanf("%d %d", &num1, num2);
    difference = (num1 - num2);
        printf("%d\n", difference);
    }
int main() {
    int num1, num2, product;
    scanf("%d %d", &num1, num2);
    sum = (num1 * num2);
        printf("%d\n", product);
    }
    
    return 0;
    
}